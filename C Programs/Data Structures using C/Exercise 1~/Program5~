5. Write a program to reverse a string and check whether string is palindrome or not.



#include<stdio.h>
#include<conio.h>
void main()
{ int i, j, k;
char str[100];
char rev[100];
printf("Enter a string\t");
scanf("%s",&str);
for(i = 0; str[i]!= '\0'; i++);
{ k = i-1; }
for(j = 0; j <= i-1; j++)
{ rev[j] = str[k];
 k--; }
if(rev==str)
printf("String is Palindrome");
else printf("String is not Palindrome");
getch(); }
